name: stage47a-47z-clinical-followup

on:
  pull_request:
    paths:
      - 'backend/self-hosted/**'
      - 'deploy/self-hosted/**'
      - 'docs/backend/**'
      - 'docs/project-memory/**'
      - 'scripts/**'
      - 'src/lib/self-hosted-follow-up-api.ts'
      - 'src/pages/doctor/VisitWorkspaceLiveActions.tsx'
      - 'package.json'
      - '.github/workflows/stage47a-47z-clinical-followup.yml'

jobs:
  stage47a-47z:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22.22.0"
      - run: npm ci
      - run: npm run preflight:stage47a-47z
